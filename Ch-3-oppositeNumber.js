// function opposite(number) {
//   if (number === 0) {
//     return number;
//   }
//   return number * -1;
// }

const opposite = (number) => (number === 0 ? number : -number);

console.log(opposite(100));
